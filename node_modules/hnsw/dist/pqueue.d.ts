export declare class PriorityQueue<T> {
    private compare;
    private items;
    constructor(compare: (a: T, b: T) => number);
    push(item: T): void;
    pop(): T | undefined;
    isEmpty(): boolean;
}
