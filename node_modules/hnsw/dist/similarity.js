"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.euclideanSimilarity = exports.cosineSimilarity = void 0;
// Note: Similarity functions
function dotProduct(a, b) {
    let dP = 0.0;
    for (let i = 0; i < a.length; i++) {
        dP += a[i] * b[i];
    }
    return dP;
}
function cosineSimilarity(a, b) {
    return dotProduct(a, b) / (Math.sqrt(dotProduct(a, a)) * Math.sqrt(dotProduct(b, b)));
}
exports.cosineSimilarity = cosineSimilarity;
function euclideanDistance(a, b) {
    let sum = 0.0;
    for (let i = 0; i < a.length; i++) {
        sum += (a[i] - b[i]) ** 2;
    }
    return Math.sqrt(sum);
}
function euclideanSimilarity(a, b) {
    return 1 / (1 + euclideanDistance(a, b));
}
exports.euclideanSimilarity = euclideanSimilarity;
